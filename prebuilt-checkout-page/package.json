{
  "name": "prebuilt-checkout-page",
  "version": "1.0.0",
  "description": "Learn how to create low-code Stripe integration to build a customized payment page, hosted on Stripe. The client- and server-side code redirects to a prebuild payment page hosted on Stripe.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "serve": "nodemon server.js",
    "debug": "*$ DEBUG=express:* node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "payment"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "stripe": "^10.8.0"
  }
}
